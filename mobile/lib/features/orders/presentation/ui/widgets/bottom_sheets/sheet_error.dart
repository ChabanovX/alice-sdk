import 'package:flutter/material.dart';

class SheetError extends StatelessWidget {
  const SheetError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
